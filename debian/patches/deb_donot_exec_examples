--- a/tools/make_examples.py
+++ b/tools/make_examples.py
@@ -87,7 +87,10 @@ if not os.path.isdir('fig'):
     
 for script in glob('*.py'):
     figure_basename = os.path.join('fig', os.path.splitext(script)[0])
-    execfile(script)
+    # Do not run the script during build of Debian package since
+    # no display would be available for VTK -- is there option for offscreen
+    # rendering?
+    # execfile(script)
     plt.close('all')
 
 
